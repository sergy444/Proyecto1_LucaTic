{
    "name": "my-project",
    "version": "0.0.0",
    "type": "module",

    "devDependencies": {
        "jest": "^27.3.1"
    },

    "jest": {
        "testEnvironment": "jsdom"
      },
    "scripts": {
        "test": "jest"
    }
}
